<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!--
        largeHeap: MITIGATION for the v0.9.73 OOM reports (#238/#239) — users
        exhaust the default 256MB Java heap ("<1% free after GC"). Doubles the
        headroom while the crash-report memory diagnostics identify what fills
        it. Remove once the root cause is fixed.
    -->
    <application
        android:name=".StashApplication"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:extractNativeLibs="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Stash">
        <!--
            No-UI trampoline launched by the "Resume playback" app shortcut
            (and Samsung Bixby Routines). Resumes the last queue and finishes
            immediately, so it uses Theme.NoDisplay and is kept out of the
            recents/back stack. exported=true so Routines can launch it.
        -->
        <!--
            Launched explicitly (by class) from the "Resume playback" static
            shortcut, so it needs no intent-filter — only exported=true so the
            launcher / Bixby Routines can start it from another process.
        -->
        <activity
            android:name=".ResumePlaybackActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:taskAffinity=""
            android:theme="@android:style/Theme.NoDisplay" />
        <activity android:name=".MainActivity" android:exported="true" android:theme="@style/Theme.Stash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!--
                Share target for importing user audio files into the Stash
                library. Users can long-press an audio file in their file
                manager (or other app) and pick Stash from the share sheet;
                MainActivity.onCreate / onNewIntent routes the URIs to the
                LocalImportCoordinator.
            -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="audio/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="audio/*" />
            </intent-filter>
            <!--
                stash://track share links (Share sheet "Stash link"): opens
                the app on Search pre-queried with the shared song.
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="stash" android:host="track" />
            </intent-filter>
            <!-- Static app shortcuts (e.g. "Resume playback"). Must live on
                 the launcher activity. -->
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>
        <receiver
            android:name="com.stash.core.data.sync.BootReceiver"
            android:exported="true"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!--
            Disable the default WorkManager Jetpack Startup initializer so that
            StashApplication.workManagerConfiguration (backed by HiltWorkerFactory)
            is used instead. Without this removal, WorkManager initializes itself
            before Hilt can inject the factory and @HiltWorker classes will fail
            to instantiate at runtime.
        -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

        <!--
            FileProvider for sharing crash reports written to
            cacheDir/crashes/ by CrashFileStore. The Settings → Diagnostics
            "Share latest crash report" button builds a content:// URI
            against this authority and grants FLAG_GRANT_READ_URI_PERMISSION
            to the recipient app (email / Discord / GitHub upload).
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
